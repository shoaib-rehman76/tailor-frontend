export const STORAGE_KEYS = {
  settings: "dtd:settings",
  offlineQueue: "dtd:offlineQueue",
  syncStatus: "dtd:syncStatus",
  authSession: "dtd:authSession",
  orders: "dtd:orders",
  customers: "dtd:customers",
  pushToken: "dtd:pushToken",
} as const;
